document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.buyerPage!=='settlement')return;const p=document.querySelector('#buyerPage'),settlements=[{id:'TXN-1024',lot:'LOT-2048',crop:'Sona Masuri Rice',verified:4.8,agreed:5,rate:3250,seller:'Farmer XYZ',delivery:'26 Sept 2026',status:'Pending Payment',subtotal:156000,fee:1560,tax:281,adjustment:0,total:157841},{id:'TXN-1028',lot:'LOT-3012',crop:'Onion',verified:10,agreed:10,rate:2450,seller:'Farmer Ramesh',delivery:'25 Sept 2026',status:'Paid',subtotal:245000,fee:2450,tax:441,adjustment:0,total:247891,paymentId:'pay_confirmed_1028'},{id:'TXN-1012',lot:'LOT-1998',crop:'Wheat',verified:7.5,agreed:8,rate:2650,seller:'Farmer Mahesh',delivery:'23 Sept 2026',status:'Payment Failed',subtotal:198750,fee:1988,tax:358,adjustment:0,total:201096}];let filter='All',query='',selected;const money=n=>'₹'+n.toLocaleString('en-IN'),cls=s=>s==='Paid'?'paid':s==='Payment Failed'?'failed':s==='Payment Processing'?'processing':'pending';
function rows(){return settlements.filter(x=>(filter==='All'||x.status===filter)&&(`${x.id} ${x.lot} ${x.crop}`).toLowerCase().includes(query.toLowerCase())).map(x=>`<tr><td data-label="Transaction"><b>${x.id}</b></td><td data-label="Lot">${x.lot}</td><td data-label="Produce">${x.crop}</td><td data-label="Verified quantity">${x.verified.toFixed(2)} T</td><td data-label="Rate">${money(x.rate)} / q</td><td data-label="Settlement amount"><b>${money(x.total)}</b></td><td data-label="Status"><span class="settle-status ${cls(x.status)}">${x.status==='Paid'?'✓ ':x.status==='Pending Payment'?'● ':'× '}${x.status}</span></td><td data-label="Action"><button class="buyer-btn ${x.status==='Pending Payment'?'primary':''}" data-open="${x.id}">${x.status==='Pending Payment'?'Pay Now':x.status==='Paid'?'View Receipt':'Try Again'}</button></td></tr>`).join('')}
function render(){p.innerHTML=`<div class="settle-page"><header class="settle-head"><div><h1>Settlement &amp; Payment</h1><p class="settle-sub">Complete payment for your verified purchase and view the final settlement details.</p></div><a class="buyer-btn" href="buyer-portal-orders.html">Settlement History</a></header><section class="settle-kpis"><button class="settle-kpi pending" data-filter="Pending Payment"><div class="buyer-kpi-header"><span class="buyer-kpi-title">PENDING PAYMENT</span><i class="fa-solid fa-wallet"></i></div><div class="buyer-kpi-value"><b>2</b><small>Ready to pay</small></div></button><button class="settle-kpi paid" data-filter="Paid"><div class="buyer-kpi-header"><span class="buyer-kpi-title">PAID</span><i class="fa-solid fa-circle-check"></i></div><div class="buyer-kpi-value"><b>18</b><small>Completed payments</small></div></button><button class="settle-kpi issue" data-filter="Payment Failed"><div class="buyer-kpi-header"><span class="buyer-kpi-title">PAYMENT ISSUES</span><i class="fa-solid fa-triangle-exclamation"></i></div><div class="buyer-kpi-value"><b>1</b><small>Requires attention</small></div></button></section><section><div class="settle-section-head"><div><h2>Pending Settlements</h2><p>Verified transactions ready for payment.</p></div></div><div class="settle-tools"><label><i class="fa-solid fa-magnifying-glass"></i><input data-search value="${query}" placeholder="Search transaction / lot / produce"></label><select data-status><option>All</option><option>Pending Payment</option><option>Payment Processing</option><option>Paid</option><option>Payment Failed</option><option>Refunded</option></select></div><table class="settle-table"><thead><tr><th>TRANSACTION</th><th>LOT</th><th>PRODUCE</th><th>VERIFIED QUANTITY</th><th>RATE</th><th>SETTLEMENT AMOUNT</th><th>STATUS</th><th></th></tr></thead><tbody>${rows()}</tbody></table></section></div>`;p.querySelector('[data-status]').value=filter}
function progress(s){const paid=s.status==='Paid';return `<div class="settle-progress"><span class="done">✓ Auction Won</span><span class="done">✓ Farmer Accepted</span><span class="done">✓ Transport Completed</span><span class="done">✓ Delivery Received</span><span class="done">✓ Quantity &amp; Quality Verified</span><span class="done">✓ Delivery Accepted</span><span class="${paid?'done':'now'}">${paid?'✓':'●'} Buyer Payment</span><span class="${paid?'now':''}">${paid?'●':'○'} Farmer Payout</span><span>○ Settlement Complete</span></div>`}
function drawer(id){selected=settlements.find(x=>x.id===id);const s=selected,paid=s.status==='Paid',failed=s.status==='Payment Failed';document.querySelector('#settleDrawer').innerHTML=`<aside class="settle-drawer-box"><button class="settle-close" data-close><i class="fa-solid fa-xmark"></i></button><span class="source-id">SETTLEMENT · ${s.id}</span><h2 class="drawer-title">${s.crop}</h2><p class="drawer-sub">${s.lot} · ${s.seller}<br>${s.delivery}</p><div class="settle-badges"><span class="settle-status paid">✓ Delivery Verified</span><span class="settle-status paid">✓ Quality Verified</span><span class="settle-status paid">✓ Quantity Verified</span><span class="settle-status ${cls(s.status)}">${s.status.toUpperCase()}</span></div>${paid?`<section class="settle-section payment-success"><h3>✓ PAYMENT SUCCESSFUL</h3><p>Payment captured successfully.<br>Payment ID: ${s.paymentId}<br>Amount paid: ${money(s.total)}</p></section>`:''}<section class="settle-section"><h3>TRANSACTION SUMMARY</h3><div class="settle-details"><div><span>Produce</span>${s.crop}</div><div><span>Lot</span>${s.lot}</div><div><span>Farmer</span>${s.seller}</div><div><span>Verified quantity</span>${s.verified.toFixed(2)} Tonnes</div><div><span>Agreed rate</span>${money(s.rate)} / q</div><div><span>Verification</span>✓ Completed</div><div><span>Verification ID</span>VRF-1024</div><div><span>Verified on</span>26 Sept · 15:42</div></div></section><section class="settle-section"><h3>QUANTITY ADJUSTMENT</h3><div class="amount-grid"><div class="amount-step"><small>AGREED</small><b>${s.agreed.toFixed(2)} T</b></div><div class="amount-step"><small>RECEIVED</small><b>${s.verified.toFixed(2)} T</b></div><div class="amount-step"><small>VERIFIED</small><b>${s.verified.toFixed(2)} T</b></div><div class="amount-step"><small>DIFFERENCE</small><b>${(s.verified-s.agreed).toFixed(2)} T</b></div></div></section><section class="settle-section"><h3>SETTLEMENT BREAKDOWN</h3><div class="breakdown"><div><span>Verified quantity (${(s.verified*10).toFixed(0)} quintals) × ${money(s.rate)} / q</span><b>${money(s.subtotal)}</b></div><div><span>Platform / service fee</span><b>${money(s.fee)}</b></div><div><span>Taxes / applicable charges</span><b>${money(s.tax)}</b></div><div><span>Other adjustments</span><b>${money(s.adjustment)}</b></div><div class="total"><b>TOTAL PAYABLE</b><b>${money(s.total)}</b></div></div></section><section class="settle-section"><h3>TRANSACTION PROGRESS</h3>${progress(s)}</section><section class="settle-section"><h3>FARMER PAYOUT</h3><div class="payout-row">${paid?'● Processing':'○ Begins after buyer payment'} · Amount due to farmer: ${money(s.subtotal)}<br><small>Buyer payment and farmer payout are separate financial events.</small></div></section>${paid?`<div class="pay-stick"><span><small>PAYMENT CAPTURED</small><b>${money(s.total)}</b></span><button class="buyer-btn" data-receipt>View Payment Receipt</button></div>`:failed?`<div class="pay-stick"><span><small>PAYMENT FAILED</small><b>${money(s.total)}</b></span><button class="buyer-btn primary" data-pay>Try Again</button></div>`:`<section class="settle-section secure-payment"><i class="fa-solid fa-lock"></i><b>Secure payment</b><br>Your payment is processed securely through Razorpay. FarmerMitra never collects card, OTP or UPI credentials.</section><div class="pay-stick"><span><small>AMOUNT TO PAY</small><b>${money(s.total)}</b></span><button class="buyer-btn primary" data-pay>Pay ${money(s.total)}</button></div>`}</aside>`;document.querySelector('#settleDrawer').classList.add('open')}
async function pay(){const s=selected,btn=document.querySelector('[data-pay]');btn.disabled=true;btn.textContent='Preparing secure checkout…';try{const order=await fetch(`/api/settlements/${encodeURIComponent(s.id)}/payment-order`,{method:'POST',headers:{'Content-Type':'application/json'}}).then(r=>{if(!r.ok)throw Error('Unable to create a payment order.');return r.json()});if(!window.Razorpay)throw Error('Razorpay Checkout could not be loaded.');const checkout=new Razorpay({key:order.keyId,amount:order.amount,currency:order.currency||'INR',name:'FarmerMitra',description:`Settlement ${s.id}`,order_id:order.orderId,handler:async response=>{await fetch(`/api/settlements/${encodeURIComponent(s.id)}/verify-payment`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(response)});showToast('Payment submitted. We are confirming its captured status.');document.querySelector('#settleDrawer').classList.remove('open')},modal:{ondismiss:()=>{btn.disabled=false;btn.textContent=`Pay ${money(s.total)}`}}});checkout.on('payment.failed',()=>{btn.disabled=false;btn.textContent=`Pay ${money(s.total)}`;showToast('Payment was not completed. You can try again.');});checkout.open()}catch(e){btn.disabled=false;btn.textContent=`Pay ${money(s.total)}`;showToast(e.message)}}
function showToast(msg){const t=document.querySelector('#settleToast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),4500)}document.body.insertAdjacentHTML('beforeend','<div class="settle-drawer" id="settleDrawer"></div><div class="settle-toast" id="settleToast"></div>');render();p.addEventListener('input',e=>{if(e.target.matches('[data-search]')){query=e.target.value;render()}});p.addEventListener('change',e=>{if(e.target.matches('[data-status]')){filter=e.target.value;render()}});p.addEventListener('click',e=>{const f=e.target.closest('[data-filter]');if(f){filter=f.dataset.filter;render()}const o=e.target.closest('[data-open]');if(o)drawer(o.dataset.open)});document.querySelector('#settleDrawer').addEventListener('click',e=>{if(e.target===e.currentTarget||e.target.closest('[data-close]'))e.currentTarget.classList.remove('open');if(e.target.closest('[data-pay]'))pay();if(e.target.closest('[data-receipt]'))showToast('Receipt download will be available from the verified settlement record.')} )});
